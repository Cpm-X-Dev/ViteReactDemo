/**
 * Activity 2: Props and Types
 *
 * Goal: Create a component that accepts typed props and renders a profile card.
 *
 * Instructions:
 * 1. Define a type for the component props (name: string, role: string, isActive: boolean)
 * 2. Complete the ProfileCard component to accept and display these props
 * 3. Render the name in an <h3>, the role in a <p>, and show "Active" or "Inactive"
 *    based on the isActive prop
 */

// TODO: Define a type called ProfileCardProps with:
//   - name: string
//   - role: string
//   - isActive: boolean

// TODO: Update the component to accept props of type ProfileCardProps
// and render:
//   <div>
//     <h3>{name}</h3>
//     <p>{role}</p>
//     <span>Active</span> or <span>Inactive</span> based on isActive
//   </div>

export const ProfileCard = () => {
    // TODO: Destructure props and render the profile card
    return null;
};
